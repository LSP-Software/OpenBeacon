{
  "name": "@openbeacon/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "test": "bun test --pass-with-no-tests"
  },
  "dependencies": {
    "better-auth": "^1.5.5"
  }
}
